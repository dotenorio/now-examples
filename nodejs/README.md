▲ NodeJS
----

To run, just execute:

```
$ npm i
$ now
```

You can use `$ npm start` to see this server locally:

```
$ npm start

> @ start path\to\now-examples\nodejs
> micro

micro: Accepting connections on port 3000
```